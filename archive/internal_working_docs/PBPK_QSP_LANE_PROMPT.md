Do a DEEP synthesis of everything the PBPK-QSP model lane has built — from first parameterization to the current whole-body runs — into two deliverables, matching the data-analysis lane so the final submission reads as one project. Only include current, truthful, validated model state; anything provisional or not artifact-backed is omitted or explicitly labeled PROVISIONAL.

**1) A publication-grade Results+Methods report section** (`QSP_REPORT_SECTION.md`) covering:
- **Architecture:** the 12-organ PBPK layer; ternary CD3×TAA(×costim) binding (QSS trimer + receptor-as-states conservation, `unified_binding.py`); the per-cell PD engine (`wholebody_pd.py`, `coupled_percell_pd.py`, `signaling_dynamics.py`) — per-cell occupancy occ=(Cd/(Cd+KD_costim))·(Rc/anchor), effector→kill gain g_eff=exp(kE_gain·eff_p) with kE_gain=0.55, suppression→Treg damping, exhaustion→kill decay; the cis/trans format gate `_apply_cis_coincidence` (p_cis emergent from span geometry: cis ≈12.5 nm gated / trans ≈60 nm decoupled — the epitope-choice knob).
- **Parameterization table:** every parameter with value, unit, source (literature DOI / HPA-Glassman copy number / fitted-with-target / assumed). No unsourced numbers.
- **Validation:** the IL-6/CRS mechanism fit (the gate before any screen) + any RTCC CITE-seq (GSE292621) calibration — show the quantitative agreement.
- **Results:** 12-arm × 2-context (Treg-rich vs Treg-poor) × CEACAM5 window sweep, drive-magnitude uncertainty (kE=0.11) propagated to error bars; the counter-screen emergence (clean arms {4-1BB, CD27} hold kill both contexts, liability-gated arms lose window in Treg-rich) as the headline figure.
- **Integration contract (state verbatim):** magnitude = analysis-lane drive table (cd4_grn_qsp_drive_full.csv, per_arm_drive_magnitude_uncertainty.csv, per_arm_prolif_exh_drive.csv); shape = shared GRN backbone operator on the three CITE-seq baselines (CD8/Treg/CD4conv, source-of-truth, CP10K dropped); timescale = hero Rest/8/48hr kinetics; the liability veto is applied UPSTREAM (QSP does not re-decide nomination).
- **Honest limitations:** (a) **static R_costim** — set once at init from resting copy numbers, read unchanged every step, so the model captures conditionality via binding geometry (cis-gate) but NOT via activation-induced receptor upregulation; a resting-copy ranking under-rates 4-1BB/OX40/ICOS and yields a spurious "CD2 wins" ordering — disclose this and, if you wire R_costim(t)=R_rest+R_induced·f(engagement), version it and source the induction kinetics from literature (don't invent). (b) hero dataset is single-target → AND-gate effects are in-silico compositional. (c) CRISPRi is LOF while an engager arm is GOF.
- **CAVEAT:** the OX40/GITR net-negative-kill-in-Treg-rich figure is PROVISIONAL (in-chat-only, not artifact-backed, structurally impossible under static R_costim) — omit or label PROVISIONAL; do not present as established.

**2) A reproducibility Jupyter notebook** (`qsp_reproduction.ipynb`) a judge runs top-to-bottom and every number reproduces: global seed; pinned versions + env snapshot; inline asserts on key outputs; step-by-step markdown before each cell explaining the biology and the modeling choice; every input loaded by pinned artifact version_id (no untraceable hard-coded numbers). Order: load inputs + show integration contract → build per-cell baselines → parameterize binding+PD → IL-6/CRS validation (show fit) → single-arm worked example (CD3×CEACAM5+clean arm, per-cell signaling across organs) → full 12-arm×2-context window sweep → counter-screen figure → verification-ledger cell that re-derives and asserts every headline number. Layer honestly: run tractable steps live; for heavy PBPK ODE integration document exact params + provide a load-from-checkpoint path so it still runs end-to-end.

**Also ship:** `QSP_VERIFICATION_LEDGER.csv` (one row per load-bearing claim: observed value, source file, PASS/FAIL — every headline number needs a row); publication-grade figures (load the figure-style skill); `requirements.txt`; a one-paragraph "validated-now vs still-underway" note so the master report can reserve space accurately. Keep the static-R_costim version reproducible even if you also ship an activation-induced version; append a clear version tag matching the analysis lane's convention.